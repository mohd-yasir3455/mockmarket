PORT=5000
# Example local MongoDB URI (uncomment and replace for local dev):
# MONGO_URI=mongodb://127.0.0.1:27017/mockmarket
# Example MongoDB Atlas URI (do NOT commit real credentials):
# MONGO_URI=mongodb+srv://<user>:<password>@cluster0.mongodb.net/mockmarket?retryWrites=true&w=majority

# NOTE: For Vercel deployments, set the MONGO_URI in the Vercel Project Settings -> Environment Variables. Do NOT commit secrets to the repo.
